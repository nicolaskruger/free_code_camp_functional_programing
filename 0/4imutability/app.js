// The global variable
var fixedValue = 4;

// Only change code below this line
/**
 * 
 * @param {number} arg 
 */
function incrementer (arg) {

    return arg+1;
  // Only change code above this line
}